# tpt-eng-examples

Cross-crate integration scenarios that compose the `tpt-eng-*` engineering
primitives the way a real physical-systems vertical would: topology → network
matrix → controls → time-series conditioning → structural check.

Dual-licensed under MIT or Apache-2.0.
